# PreSignUp Lambda runtime deps. Empty for the v1 stub (only stdlib).
# Real T-4.8 will add httpx for /internal/check_invite calls and boto3 for SSM
# (boto3 is provided by the Lambda runtime; httpx must be packaged).
